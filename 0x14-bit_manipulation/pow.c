#include <math.h>
#include <stdio.h>
int main(void) {
	int a = 0;
	double result = 3 * pow(4, 5);

	//printf("enter exponent number: ");
	//scanf("%d", &a);
	printf("\n3 * %d^%d is = %lf\n", 4, 5, result);


	return 0;
}
/*
#include <math.h>
#include <stdio.h>

int main(void)
{
    double result = pow(4, 3);
    printf("4 raised to the power of 3 is %lf\n", result);
    return 0;
}
*/
